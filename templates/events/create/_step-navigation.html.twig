<div class="mb-3 flex items-center justify-around">
    <a class="w-full rounded-lg px-5 py-2.5 text-center text-sm font-medium {{ app.request.attributes.get('_route_params')|first == 'details' ? 'bg-blue-700 text-white' : 'border border-blue-700 text-blue-700 hover:bg-blue-700 hover:text-white' }}"
       href="{{ path('create_event') }}">
        <div class="bi bi-text-left text-xl"></div>
        <div>{{ 'details'|trans|title }}</div>
    </a>
    <a class="mx-3 w-full rounded-lg px-5 py-2.5 text-center text-sm font-medium {{ app.request.attributes.get('_route_params')|first == 'location' ? 'bg-blue-700 text-white' : 'border border-blue-700 text-blue-700 hover:bg-blue-700 hover:text-white' }}"
       href="{{ path('create_event') }}">
        <div class="bi bi-geo text-xl"></div>
        <div>{{ 'location'|trans|title }}</div>
    </a>
</div>

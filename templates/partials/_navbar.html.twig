<nav class="border-b border-primary-800 bg-primary-600">
    <div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-3 sm:px-6 lg:px-8">
        <a class="flex items-center gap-3 no-underline" href="{{ path('events') }}">
            <img class="h-10" src="{{ asset('images/logo-90-white-01.png') }}" alt="Event Point">
            <span class="hidden text-lg font-semibold tracking-tight text-white lg:block">Event Point</span>
        </a>
        <ul class="flex list-none items-center gap-4">
            <li>
                <a class="inline-flex items-center gap-2 rounded-full bg-white px-4 py-2 text-sm font-medium text-primary-600 no-underline transition-all hover:bg-primary-50 hover:shadow-md" href="{{ path('create_event') }}">
                    <span class="hidden sm:inline">{{ 'add-event'|trans }}</span>
                    <span class="sm:hidden">
                        {{ ux_icon('mdi:plus', {class: 'text-xl'}) }}
                    </span>
                </a>
            </li>
            <li>
                <a class="inline-flex h-10 w-10 cursor-pointer items-center justify-center rounded-full text-white/70 transition-colors hover:bg-primary-700 hover:text-white" href="{{ path('events') }}">
                    {{ ux_icon('mdi:compass-outline', {class: 'text-3xl'}) }}
                </a>
            </li>
            {% if app.user %}
                <li>
                    <a class="inline-flex h-10 w-10 items-center justify-center rounded-full text-white/70 transition-colors hover:bg-primary-700 hover:text-white" href="{{ path('user_event_invitations') }}">
                        {{ ux_icon('mdi:account-circle-outline', {class: 'text-3xl'}) }}
                    </a>
                </li>
            {% endif %}
            <li>
                <a class="inline-flex h-10 w-10 cursor-pointer items-center justify-center rounded-full text-white/70 transition-colors hover:bg-primary-700 hover:text-white" data-modal-target="regional_settings" data-modal-toggle="regional_settings">
                    {{ ux_icon('mdi:web', {class: 'text-3xl'}) }}
                </a>
            </li>
            <li>
                <a class="inline-flex h-10 w-10 cursor-pointer items-center justify-center rounded-full text-white/70 transition-colors hover:bg-primary-700 hover:text-white" data-drawer-target="menu" data-drawer-show="menu" data-drawer-placement="right">
                    {{ ux_icon('mdi:menu', {class: 'text-3xl'}) }}
                </a>
            </li>
        </ul>
    </div>
</nav>


{# Drawer menu #}
<div id="menu" class="fixed right-0 top-0 z-40 h-screen w-80 translate-x-full overflow-y-auto border-l border-gray-100 bg-white shadow-2xl transition-transform" tabindex="-1" aria-labelledby="menu">
    <div class="flex h-full flex-col">
        <div class="flex items-center justify-between border-b border-gray-100 p-5">
            <span class="text-sm font-medium uppercase tracking-wider text-gray-400">{{ 'menu'|trans }}</span>
            <button type="button" data-drawer-hide="menu" aria-controls="menu" aria-label="{{ 'close'|trans }}" class="inline-flex h-8 w-8 items-center justify-center rounded-full text-gray-400 transition-colors hover:bg-gray-100 hover:text-gray-600">
                <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
                </svg>
            </button>
        </div>
        {% if app.user %}
            <a class="flex items-center gap-4 border-b border-gray-100 p-5 no-underline transition-colors hover:bg-gray-50"
               href="{{ path('user_future_events') }}">
                <img src="{{ user_avatar(app.user) }}" class="h-12 w-12 rounded-full object-cover ring-2 ring-gray-100" alt="">
                <div class="min-w-0 flex-1">
                    <div class="truncate font-medium text-gray-900">{{ app.user.fullName }}</div>
                    <div class="truncate text-sm text-gray-500">{{ app.user.email.address }}</div>
                </div>
            </a>
        {% endif %}
        <nav class="flex-1 py-2">
            {% if app.user %}
                <a href="{{ path('events') }}"
                   class="flex items-center gap-3 px-5 py-3 text-gray-700 no-underline transition-colors hover:bg-gray-50 hover:text-gray-900">
                    {{ ux_icon('mdi:calendar-search', {class: 'text-lg text-gray-400'}) }}
                    <span>{{ 'all-events'|trans }}</span>
                </a>
                <a href="{{ path('user_future_events') }}"
                   class="flex items-center gap-3 px-5 py-3 text-gray-700 no-underline transition-colors hover:bg-gray-50 hover:text-gray-900">
                    {{ ux_icon('mdi:view-dashboard-outline', {class: 'text-lg text-gray-400'}) }}
                    <span>{{ 'dashboard'|trans }}</span>
                </a>
                <div class="my-2 border-t border-gray-100"></div>
                <a href="{{ path('create_event', {step: 'details'}) }}"
                   class="flex items-center gap-3 px-5 py-3 text-gray-700 no-underline transition-colors hover:bg-gray-50 hover:text-gray-900">
                    {{ ux_icon('mdi:calendar-plus', {class: 'text-lg text-gray-400'}) }}
                    <span>{{ 'create-event'|trans }}</span>
                </a>
                <div class="my-2 border-t border-gray-100"></div>
                <a href="{{ path('user_account') }}"
                   class="flex items-center gap-3 px-5 py-3 text-gray-700 no-underline transition-colors hover:bg-gray-50 hover:text-gray-900">
                    {{ ux_icon('mdi:cog-outline', {class: 'text-lg text-gray-400'}) }}
                    <span>{{ 'account'|trans }}</span>
                </a>
            {% else %}
                <a href="{{ path('app_register') }}"
                   class="flex items-center gap-3 px-5 py-3 text-gray-700 no-underline transition-colors hover:bg-gray-50 hover:text-gray-900">
                    {{ ux_icon('mdi:account-plus-outline', {class: 'text-lg text-gray-400'}) }}
                    <span>{{ 'register'|trans }}</span>
                </a>
                <a href="{{ path('app_login') }}"
                   class="flex items-center gap-3 px-5 py-3 text-gray-700 no-underline transition-colors hover:bg-gray-50 hover:text-gray-900">
                    {{ ux_icon('mdi:login', {class: 'text-lg text-gray-400'}) }}
                    <span>{{ 'login'|trans }}</span>
                </a>
            {% endif %}
        </nav>
        {% if app.user %}
            <div class="border-t border-gray-100 p-5">
                <a href="{{ path('app_logout') }}"
                   class="flex w-full items-center justify-center gap-2 rounded-lg border border-gray-200 py-2.5 text-sm font-medium text-gray-600 no-underline transition-colors hover:bg-gray-50 hover:text-gray-900">
                    {{ ux_icon('mdi:logout', {class: 'text-base'}) }}
                    <span>{{ 'logout'|trans }}</span>
                </a>
            </div>
        {% endif %}
    </div>
</div>


{# Regional settings modal #}
<div id="regional_settings" tabindex="-1" aria-hidden="true"
     class="fixed left-0 right-0 top-0 z-50 hidden h-[calc(100%-1rem)] max-h-full w-full overflow-y-auto overflow-x-hidden p-4 md:inset-0">
    <div class="relative max-h-full w-full max-w-md">
        <div class="relative overflow-hidden rounded-2xl bg-white shadow-xl ring-1 ring-gray-100">
            <div class="flex items-center justify-between border-b border-gray-100 px-6 py-4">
                <h5 class="text-base font-semibold text-gray-900">{{ 'regional-settings'|trans }}</h5>
                <button type="button" class="inline-flex h-8 w-8 items-center justify-center rounded-full text-gray-400 transition-colors hover:bg-gray-100 hover:text-gray-600" data-modal-hide="regional_settings">
                    <svg class="h-4 w-4" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
                    </svg>
                    <span class="sr-only">{{ 'close'|trans }}</span>
                </button>
            </div>
            <turbo-frame id="regional_settings_frame" src="{{ path('_app_regional_settings') }}"
                         target="_top">
                <div class="flex min-h-[40vh] items-center justify-center">
                    <div class="h-8 w-8 animate-spin rounded-full border-2 border-gray-200 border-t-gray-600" role="status">
                        <span class="sr-only">{{ 'loading'|trans }}</span>
                    </div>
                </div>
            </turbo-frame>
        </div>
    </div>
</div>
